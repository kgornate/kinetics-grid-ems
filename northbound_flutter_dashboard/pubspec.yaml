name: northbound_flutter_dashboard
version: 0.8.0+8
description: NorthBound EMS Gateway dashboard with operator asset fields, historian, gateway events, storage, and asset-specific UI views.
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
