# ==========================================================
# EMS Wi-Fi Boot Configuration - FRDM-i.MX93
# ==========================================================

WIFI_ENABLED="1"
WIFI_IFACE="mlan0"
WIFI_COUNTRY="IN"

# Preferred Wi-Fi
WIFI_PRIMARY_SSID="OnePlus 13s CF94"
WIFI_PRIMARY_PASSWORD="Finland@12"

# Backup Wi-Fi
WIFI_BACKUP_SSID="BESS_Ornate 1"
WIFI_BACKUP_PASSWORD="BESS@2026"

# DHCP and retry behaviour
WIFI_CONNECT_ATTEMPTS="3"
WIFI_ASSOC_TIMEOUT_SEC="30"
WIFI_DHCP_ATTEMPTS="5"
WIFI_DHCP_TIMEOUT_SEC="5"

# DNS fallback used only if DHCP does not populate resolv.conf correctly
DNS1="1.1.1.1"
DNS2="8.8.8.8"

# Runtime log
WIFI_LOG="/var/log/ems_wifi_boot.log"
