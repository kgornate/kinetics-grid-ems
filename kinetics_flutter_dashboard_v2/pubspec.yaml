name: kinetics_gateway_dashboard
description: Kinetics Gateway V2 read-only BMS and PCS monitoring dashboard.
publish_to: 'none'
version: 3.0.0+8

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
